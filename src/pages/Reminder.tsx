export default function Reminder() {
  return (
    <div>
      <h1>Reminder</h1>
      <p>
        This is private page. Change the auth status from{' '}
        <code>/src/utils/PrivateRoute.tsx</code> to toggle.
      </p>
    </div>
  )
}
